rule all:
    input:
        "results/figure1.pdf"

rule sim_naive:
    input:
        "sim.R"
    output:
        "results/sim-naive.rds"
    shell:
        "Rscript sim.R naive {output}"

rule sim_split:
    input:
        "sim.R"
    output:
        "results/sim-split.rds"
    shell:
        "Rscript sim.R split {output}"

rule figure:
    input:
        script = "figure.R",
        rds = ["results/sim-naive.rds", "results/sim-split.rds"]
    output:
        "results/figure1.pdf"
    shell:
        "Rscript {input.script} {output} {input.rds}"
